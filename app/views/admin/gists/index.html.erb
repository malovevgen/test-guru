
<h1><%= t('.header') %></h1>

<div class="table-resposive">
  <table class="table mt-3">
    <thead>
      <tr>
        <th><%= t('.question') %></th>
        <th><%= t('.link') %></th>
        <th><%= t('.user') %></th>
      </tr>
    </thead>

    <%= render @gists %>
  </table>
</div>
<%= link_to 'Back', admin_tests_path %>
