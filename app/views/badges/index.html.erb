<h1><%= t('.head') %></h1>

  <title>table-layout</title>
  <style>
   table {
    table-layout: fixed; /* Фиксированная ширина ячеек */
    width: 50%; /* Ширина таблицы */
   }
  </style>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.description') %> </th>
    <th><%= t('.image') %></th>
  </tr>

  <% @badges.select { |badge| badge.status }.each do |badge| %>
    <tr>
      <td><%= badge.title %></td>
      <td><%= badge.description %> </td>
      <td><%= image_tag("badges/#{badge.image_name}", width:'100px') %></td>
    </tr>
  <% end %>
</table> 

<h1><%= t('.head') %></h1>

  <title>table-layout</title>
  <style>
   table {
    width: 50%;
   }
  </style>

<table>
  <tr>
    <th><%= t('.acquired') %></th>
    <th><%= t('admin.shared.badges') %></th>
  </tr>

  <% current_user.badges.each do |badge| %>
    <tr>
      <td><%= badge.created_at %></td>
      <td><%= image_tag("badges/#{Badge.find(badge.id).image_name}", width:'100px') %></td>
    </tr>
  <% end %>
</table>     

<%= link_to t('admin.shared.back'), tests_path %>
