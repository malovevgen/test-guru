<p class="nav user"><%= link_to t('.header'), tests_path %></p>
<% if user_signed_in? %>
   <p class="nav user">
    <%= t('shared.navbar.welcome')%>, <b><%= current_user.first_name %></b>
    <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
  </p>
<% else %>
  <p class="nav user">
    <%= link_to t('.sign_up'), new_user_registration_path %> |
    <%= link_to t('.sign_in'), new_user_session_path %>
  </p>
<% end %>
